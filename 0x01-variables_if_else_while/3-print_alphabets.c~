#include <stdio.h>
/**
* main - Entry point
* for loop
* Return: Always 0 (Success)
*/
int main(void)
{
int c;
for (c = 'a'; c <= 'z'; c++)
{
putchar(c);
}
for (c ='A'; c <= 'Z'; c++)
{
putchar(c);
}
